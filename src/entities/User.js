class User {
    constructor(userId, email, firstName, lastName, role) {
        this.userId = userId;
        this.email = email;
        this.firstName = firstName;
        this.lastName = lastName;
        this.role = role;
    }
}

// j'exporte la classe
export default User;